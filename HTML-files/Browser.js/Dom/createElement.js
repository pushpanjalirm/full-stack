let para = document.createElement('p'); //created para but not added to index html

para.innerText = 'Dynamic Element';
document.body.appendChild(para) //append will add the text to its last child
console.log(para);

// create array of hobbies and display it

var hobbies = ['football']

//  function walkdown(ele) {
    // do{

    // }while()
// }